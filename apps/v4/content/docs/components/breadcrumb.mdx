---
title: فتات الخبز
description: يعرض المسار إلى المورد الحالي باستخدام تدرج هرمي من الروابط.
component: true
---

<ComponentPreview
  name="breadcrumb-demo"
  className="[&_.preview]:p-2"
  description="فتات خبز مع قائمة منسدلة قابلة للطي."
/>

## التثبيت

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">يدوي</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add breadcrumb
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>انسخ والصق الكود التالي في مشروعك.</Step>

<ComponentSource name="breadcrumb" title="components/ui/breadcrumb.tsx" />

<Step>قم بتحديث مسارات الاستيراد لتطابق إعداد مشروعك.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## الاستخدام

```tsx showLineNumbers
import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb"
```

```tsx showLineNumbers
<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink href="/">الرئيسية</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbLink href="/components">المكونات</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbPage>فتات الخبز</BreadcrumbPage>
    </BreadcrumbItem>
  </BreadcrumbList>
</Breadcrumb>
```

## أمثلة

### فاصل مخصص

استخدم مكونًا مخصصًا كـ `children` لـ `<BreadcrumbSeparator />` لإنشاء فاصل مخصص.

<ComponentPreview
  name="breadcrumb-separator"
  description="فتات خبز مع فاصل مخصص"
/>

```tsx showLineNumbers {1,10-12}
import { SlashIcon } from "lucide-react"

...

<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink href="/">الرئيسية</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator>
      <SlashIcon />
    </BreadcrumbSeparator>
    <BreadcrumbItem>
      <BreadcrumbLink href="/components">المكونات</BreadcrumbLink>
    </BreadcrumbItem>
  </BreadcrumbList>
</Breadcrumb>
```

---

### قائمة منسدلة

يمكنك تكوين `<BreadcrumbItem />` مع `<DropdownMenu />` لإنشاء قائمة منسدلة في فتات الخبز.

<ComponentPreview
  name="breadcrumb-dropdown"
  className="[&_.preview]:p-2"
  description="فتات خبز مع قائمة منسدلة."
/>

```tsx showLineNumbers {1-6,11-21}
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu"

...

<BreadcrumbItem>
  <DropdownMenu>
    <DropdownMenuTrigger>
      المكونات
    </DropdownMenuTrigger>
    <DropdownMenuContent align="start">
      <DropdownMenuItem>التوثيق</DropdownMenuItem>
      <DropdownMenuItem>السمات</DropdownMenuItem>
      <DropdownMenuItem>GitHub</DropdownMenuItem>
    </DropdownMenuContent>
  </DropdownMenu>
</BreadcrumbItem>
```

---

### مطوي

نحن نوفر مكون `<BreadcrumbEllipsis />` لإظهار حالة مطوية عندما يكون فتات الخبز طويلاً جدًا.

<ComponentPreview
  name="breadcrumb-ellipsis"
  className="[&_.preview]:p-2"
  description="فتات خبز يظهر حالة مطوية."
/>

```tsx showLineNumbers {1,9}
import { BreadcrumbEllipsis } from "@/components/ui/breadcrumb"

...

<Breadcrumb>
  <BreadcrumbList>
    {/* ... */}
    <BreadcrumbItem>
      <BreadcrumbEllipsis />
    </BreadcrumbItem>
    {/* ... */}
  </BreadcrumbList>
</Breadcrumb>
```

---

### مكون الرابط

لاستخدام مكون رابط مخصص من مكتبة التوجيه الخاصة بك، يمكنك استخدام خاصية `asChild` على `<BreadcrumbLink />`.

<ComponentPreview
  name="breadcrumb-link"
  description="فتات خبز مع مكون رابط مخصص"
/>

```tsx showLineNumbers {1,8-10}
import Link from "next/link"

...

<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink asChild>
        <Link href="/">الرئيسية</Link>
      </BreadcrumbLink>
    </BreadcrumbItem>
    {/* ... */}
  </BreadcrumbList>
</Breadcrumb>
```

---

### متجاوب

إليك مثال على فتات خبز متجاوب يكوّن `<BreadcrumbItem />` مع `<BreadcrumbEllipsis />`، `<DropdownMenu />`، و `<Drawer />`.

يعرض قائمة منسدلة على سطح المكتب ودرجًا على الهاتف المحمول.

<ComponentPreview
  name="breadcrumb-responsive"
  className="[&_.preview]:p-2"
  description="فتات خبز متجاوب. يعرض قائمة منسدلة على سطح المكتب ودرجًا على الهاتف المحمول."
/>
